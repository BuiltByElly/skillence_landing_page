[project]
name = "backend"
version = "0.1.0"
description = "Skillence backend"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "aiosqlite>=0.22.1",
    "alembic>=1.18.4",
    "asyncpg>=0.31.0",
    "fastapi-offline>=1.7.6",
    "fastapi[standard]>=0.137.1",
    "greenlet>=3.5.1",
    "loguru>=0.7.3",
    "pwdlib[argon2]>=0.3.0",
    "pyjwt>=2.13.0",
    "slowapi>=0.1.10",
    "sqlmodel>=0.0.38",
    "uvicorn>=0.49.0",
]

[dependency-groups]
dev = [
    "aiosqlite>=0.22.1",
    "httpx>=0.28.1",
    "pytest>=9.1.0",
    "pytest-asyncio>=1.4.0",
    "pytest-cov>=7.1.0",
    "ruff>=0.15.17",
    "viztracer>=1.1.1",
]
[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["app/tests"]

[tool.ruff]
exclude = [
    "migrations"
]
